package ru.vsu.but.project.SimpleShipBattleProgram;public class SimpleDotComGame {    public static void main(String[] args) {        int numOfGuesses = 0; //хранит кол-во ходов пользователя        GameHelper helper = new GameHelper(); //спец класс для приема пользовательского ввода        SimpleDotCom theDotCom = new SimpleDotCom(); // создаем объект Сайт        int randomNum = (int) (Math.random() * 5);//генерация случ числа для первой ячейки        int[] locations = {randomNum, randomNum+1, randomNum+2};//формируем массив ячеек        theDotCom.setLocationCells(locations); //передеаем Сайту местоположение его ячеек(массив)        boolean isAlive = true; //переменная-проверка в цикле(не закончилась ли игра)        while (isAlive==true) {            //получаем строку введенную пользователем            String userGuess = helper.getUserInput("Введите число");            //проверяем введенные данные с расположение ячеек и return результат в переменную типа String result            String result = theDotCom.checkYourself(userGuess);            numOfGuesses++;            if(result.equals("Потопил")) {                isAlive = false;                System.out.println("Вам потребовалось "+ numOfGuesses + " попыток(и)");            }        }    }}